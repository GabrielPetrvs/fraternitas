<!DOCTYPE html>
<html lang="pt-BR">
<head><meta charset="UTF-8"><title>Fraternitas - depoimento</title><link rel="stylesheet" href="/css/style.css"></head>
<body>
<% if (usuario) { %><div id="header"><div id="header-inner"><a href="/inicio" id="logo">Fraternitas</a><form action="/buscar" method="GET" id="search-form"><input type="text" name="q" placeholder="buscar pessoas..."><button type="submit">buscar</button></form><nav><a href="/inicio">início</a><a href="/perfil">meu perfil</a><a href="/amigos">amigos</a><a href="/comunidades">comunidades</a><a href="/sair">sair</a></nav></div></div><% } %>
<div id="page">
<div style="max-width:520px;">
  <div class="panel">
    <div class="panel-header">escrever depoimento para <%= para.nome %></div>
    <div class="panel-body">
      <form method="POST" action="/depoimento/enviar">
        <input type="hidden" name="para_user_id" value="<%= para.id %>">
        <div class="form-group">
          <label>depoimento</label>
          <textarea name="texto" required style="height:100px" placeholder="escreva algo legal sobre <%= para.nome %>..."><%= existente ? existente.texto : '' %></textarea>
        </div>
        <div class="form-group"><label>confiabilidade</label>
          <select name="confiabilidade">
            <% for(var i=1;i<=5;i++) { %><option value="<%= i %>" <%= existente && existente.confiabilidade==i ? 'selected' : '' %>><%= '★'.repeat(i) + '☆'.repeat(5-i) %></option><% } %>
          </select>
        </div>
        <div class="form-group"><label>criatividade</label>
          <select name="criatividade">
            <% for(var i=1;i<=5;i++) { %><option value="<%= i %>" <%= existente && existente.criatividade==i ? 'selected' : '' %>><%= '★'.repeat(i) + '☆'.repeat(5-i) %></option><% } %>
          </select>
        </div>
        <div class="form-group"><label>sexualidade</label>
          <select name="sexualidade">
            <% for(var i=1;i<=5;i++) { %><option value="<%= i %>" <%= existente && existente.sexualidade==i ? 'selected' : '' %>><%= '★'.repeat(i) + '☆'.repeat(5-i) %></option><% } %>
          </select>
        </div>
        <button type="submit" class="btn btn-primary"><%= existente ? 'atualizar' : 'enviar' %> depoimento</button>
        <a href="/perfil/<%= para.id %>" class="btn btn-secondary" style="margin-left:6px;">cancelar</a>
      </form>
    </div>
  </div>
</div>
</div>
<div id="footer">© 2025 Fraternitas</div>
</body></html>
